#include <stdio.h>#include <stdlib.h>#include <math.h>#include <time.h>void usage(char *str){  fprintf(stderr, "usage: %s <begin> <end>\n", str);}void main(int argc, char *argv[]){  int i;  int a, b;  int rnd;  time_t t;  if(argc != 3) {    usage(argv[0]);    exit(-1);  }  a = atoi(argv[1]);  b = atoi(argv[2]);  if(a >= b || a < 0 || b < 0) {    usage(argv[0]);    exit(-1);  }    srandom(time(&t));  rnd = a + random() % (b - a);  fprintf(stdout, "random %d\n", rnd);  }  